CREATE AND RUN MARIADB
1 - run mariadb from compose file
    docker-compose -f docker-compose-mariadb.yml up
2 - connect as bellow
	localhost:<port>
	do not specify the db name, leave it blank
	root
	W00tW00tIAmR00t
	once in create the db my-cards